<!-- pages/bills/bills.wxml - 账单列表（多项目模式） -->
<view class="page">
  <!-- 筛选栏 -->
  <view class="filter-bar">
    <picker mode="date" fields="month" value="{{filterMonth}}" bindchange="onMonthChange">
      <view class="filter-month">{{filterMonthText}}</view>
    </picker>
    <view class="filter-summary">
      <text>共 {{totalCount}} 笔</text>
      <text class="amount filter-amount">¥{{totalAmount}}</text>
    </view>
  </view>

  <!-- 账单列表 -->
  <block wx:if="{{bills.length > 0}}">
    <view class="bill-group" wx:for="{{groupedBills}}" wx:key="date" wx:for-item="group">
      <view class="group-header">
        <text class="group-date">{{group.date}}</text>
        <text class="group-total amount">¥{{group.total}}</text>
      </view>
      
      <view class="bill-item" wx:for="{{group.items}}" wx:key="id" bindtap="goEdit" data-id="{{item.id}}" bindlongpress="onLongPress" data-id="{{item.id}}">
        <!-- 多项目明细 -->
        <view class="bill-items-row">
          <view class="bill-sub-item" wx:for="{{item.items}}" wx:key="id" wx:for-item="sub">
            <view class="sub-dot chip-{{sub.category}}"></view>
            <text class="sub-cat">{{sub.categoryLabel}}</text>
            <text class="sub-amount">¥{{sub.amountText}}</text>
          </view>
        </view>
        <!-- 底部信息 -->
        <view class="bill-footer">
          <view class="bill-left">
            <text class="bill-time">{{item.timeText}}</text>
            <text class="bill-note" wx:if="{{item.note}}">{{item.note}}</text>
          </view>
          <text class="bill-total amount">¥{{item.totalText}}</text>
        </view>
      </view>
    </view>
  </block>

  <view class="empty-state" wx:else>
    <text>📝</text>
    <text>还没有账单记录</text>
  </view>

  <!-- 悬浮添加按钮 -->
  <view class="fab" bindtap="goAdd">
    <text class="fab-icon">+</text>
  </view>
</view>
